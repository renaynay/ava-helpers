# Helpers for AVA Local Testnet

Send curl command against AVA nodes to generate account addresses and subsequently send transactions (for the purpose of generating chain data).

## Usage

```bash

```